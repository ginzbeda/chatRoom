from socket import *
SERVER_NAME = 'chat'
SERVER_PORT = 3000

# def connect():
#     sock = socket(AF_INET, SOCK_STREAM)
#     sock.connect(SERVER_NAME, SERVER_PORT)
#     return sock

# def send(sock, message)


# def recv(sock)

def client():
    # socket = connect()
    message = input()
    print(message)

client()